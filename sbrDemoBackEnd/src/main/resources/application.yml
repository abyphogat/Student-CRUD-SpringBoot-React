server:
  port: 9192

spring:
  datasource:
    username: root
    password: Abhay@12390
    url: jdbc:mysql://localhost:3306/sbd_db
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect